# Session 01 — Local Setup + First LLM Call

This session is the foundation for *any* LLM use case.

## Goals

By the end of this session, you should be able to:

- clone a GitHub repository locally
- create a Python virtual environment
- store your OpenAI API key safely (in a `.env` file)
- run a small script that makes a remote call to an LLM

## 1) Clone the repo

```bash
git clone <REPO_URL>
cd <REPO_FOLDER>
```

## 2) Create the environment

We use **uv**...
